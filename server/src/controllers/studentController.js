const responseData = require("../utils/response");
const { isNumber, lengthName, isString } = require("../helpers/funcAux");

const getAllStudent = () => {
  return;
};

const getIdStudent = (idUser) => {
  return;
};

const getPageStudent = (page) => {};

const createStudent = (
  idLevel,
  idExtension,
  nameStudent,
  lastNameStudent,
  emailStudent,
  carnetStudent,
  addressStudent,
  dateBirthStudent,
  photoStudent
) => {};

const updateStudent = () => {
  return;
};

const removeStudent = (idUser) => {
  return;
};

module.exports = {
  createStudent,
  getAllStudent,
  getIdStudent,
  getPageStudent,
  updateStudent,
  removeStudent,
};
